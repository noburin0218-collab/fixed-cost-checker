# DESIGN.md — 家計の保健室

このファイルがデザインの唯一の基準（Single Source of Truth）です。
値はすべて **Phase 1・2・3 で実際に実装したもの**で、`styles.css` / `articles.css` と一致します。
迷ったらここに戻り、ここを変えてからコードを変えてください。

参考にした思想（値ではなく考え方のみ）：warm off-white の地、near-black の本文、
差し色を1色に絞る、hairline の罫線、影を使わない、余白と情報階層で信頼をつくる。
特定サービスのUI・ロゴ・コピー・フォントは模倣しません。

---

## Brand Principles

1. **売らない場所に見えること。** 判断は最後まで読者のもの。
2. **紙の帳票の作法**（罫線・余白・等幅数字）で階層をつくる。影に頼らない。
3. **数字は控えめに、根拠は大きく。** 金額より「なぜそうなるか」を先に出す。
4. **正直さは最小フォントで書かない。** 免責・広告表記・出典は読める大きさで。
5. **医療の様式は借りるが、医学的判断はしない。** 危険・異常・要治療は使わない。
6. **迷ったら、要素を足さずに引く。**

ブランドの中心は明朝フォントそのものではなく、**余白・罫線・色・帳票構造・文体**です。
端末に明朝が無くても、この5つが揃っていればブランドは成立します。

---

## Voice & Tone

- 一人称は使わない。読者を「あなた」と呼ぶ。
- 断定しない：「〜な場合があります」「〜しだいで変わります」。
- 煽らない：「損しています」「今すぐ」「絶対」は使わない。
- 逃げない：不利な情報（無料相談がなぜ無料か）を先に書く。
- 「カモられない」はブランドの思想として持つが、**連呼しない**。
  使うのは立場を説明する文脈まで（例：「カモられないために、広告や相談より先に、
  自分の数字と選択肢を整理します。」）。H1・見出し・広告の近くでは使わない。
- 敬語は丁寧語まで。絵文字は使わない。感嘆符は原則使わない。

### 確定コピー

| 場所 | 文言 |
|---|---|
| ブランド名 | 家計の保健室 |
| H1（トップ） | 家計の不安、まず正体をはっきりさせる。 |
| サブコピー | 売りたい人の話を聞く前に、自分の数字を見る。30秒の家計チェックから。 |
| 主CTA | 家計チェックをはじめる |
| 結果の名称 | 家計カルテ |

---

## Color

役割で定義し、意味を1つに固定する。**この11色以外を増やさない。**

```css
--paper:       #faf9f6;  /* ページ地（warm off-white） */
--surface:     #ffffff;  /* カード・帳票面 */
--ink:         #141a17;  /* 本文・見出し・すべての金額（対 paper 16.76:1） */
--ink-2:       #4d574f;  /* 副次テキスト（対 paper 7.14:1） */
--ink-3:       #616b64;  /* 注記・メタ（対 paper 5.26:1／AAの下限として使う） */
--rule:        #d8dcd8;  /* 区切り罫。装飾のみ。操作部品には使わない */
--rule-strong: #828b85;  /* 操作部品の境界（対 surface 3.51:1／WCAG 1.4.11） */
--rule-dark:   #1d2a24;  /* 表題部の太罫 */
--seal:        #0f5c43;  /* ブランド緑：CTA・リンク・状態の印（対 surface 7.98:1） */
--seal-tint:   #eef3f0;  /* 広告帯・選択状態・表の見出しの淡面 */
--vermilion:   #a33b23;  /* 朱：印章にのみ使う */
```

**朱（--vermilion）は印章専用。** 金額・ゲージ・状態・警告には使わない。
不安を煽る赤を持たないことが、このサイトの中立性の視覚的な根拠です。

**禁止**：グラデーション／紫／純青／彩度の高い赤／ブランド緑以外のアクセント。

---

## Typography

```css
--serif: "Hiragino Mincho ProN", "Yu Mincho", YuMincho, "Noto Serif JP", serif;
--sans:  "Hiragino Sans", "Noto Sans JP", "Yu Gothic", Meiryo, system-ui, sans-serif;
```

**Webフォントは追加しない。** Core Web Vitals・配信・ライセンス・端末差を増やすほどの
優先度はないと判断済み。明朝が出ない端末ではゴシックにフォールバックしてよい。
（実データとデバイス検証の後に、別途判断する）

| 役割 | サイズ | 行間 | 書体 | 使う場所 |
|---|---|---|---|---|
| display | 1.75rem（≥600px: 2.05rem） | 1.45 | serif | ページH1 |
| title | 1.2rem | 1.45 | serif | `.section-title` |
| article-title | 1.6rem（≥600px: 1.95rem） | 1.5 | serif | 記事H1 |
| heading | 1.0〜1.22rem | 1.5〜1.6 | serif / sans | 記事h2・h3 |
| body | 1.0rem（16px） | 1.8〜1.95 | sans | 本文・記事 |
| ui | 0.9〜0.95rem | 1.7 | sans | フォーム・ボタン |
| meta | 0.82〜0.88rem | 1.8 | sans | メタ・注記・出典・広告ラベル |

- **0.8rem 未満を文章に使わない。** ラベル1語のみ 0.78rem まで許容。
- 数値は `font-variant-numeric: tabular-nums`。
- **推定値は有効数字2桁に丸める**（`roundedYen()`）。実測値・入力値は丸めない。
  368,820 → 約37万円／30,735 → 約3.1万円／7,675 → 約7,700円。

---

## Spacing

4pxグリッド：`4 / 6 / 8 / 10 / 12 / 14 / 16 / 18 / 20 / 24 / 28 / 32 / 40 / 48`

- カード内：28px 24px（≥600px は 32px 30px）
- カード間：20px
- 罫線で仕切る行：上下 10〜16px
- 本文左右：20px（`.container` の padding）

---

## Layout

```css
.container { max-width: 680px; padding: 0 20px; }
```

- **1カラムのみ。** サイドバーを作らない。≥600px でも段を増やさない。
- 増えるのは余白と、フォームの入力欄が2列になることだけ。
- 本文は1行およそ40字（≥600px）。日本語の可読上限に近い値なので広げない。
- ブレークポイントは **599px / 600px の2つ**だけ。

---

## Radius

```css
--radius: 2px;  /* 唯一の値 */
```

丸ボタン・ピル・カプセルは作らない。`border-radius: 50%` は印章（`.seal-mark`）のみ。

---

## Borders

| 用途 | 指定 |
|---|---|
| 区切り（装飾） | `1px solid var(--rule)` |
| 操作部品の境界 | `1px solid var(--rule-strong)` ← 3:1 を満たすのはこちら |
| 表題部の太罫 | `2px solid var(--rule-dark)` |
| 引用・強調の左罫 | `2px solid var(--seal)` |
| 広告セクションの枠 | `1px solid var(--rule-strong)` ＋ `--seal-tint` の地 |

**入力欄・選択肢・ボタン・開閉に `--rule` を使わない。**

---

## Elevation

`box-shadow` を使わない。例外は `:focus-visible` の outline のみ。
階層は「地色 → 罫線 → 余白 → 文字サイズ」の順で表す。

---

## Links

```css
a, a:visited { color: var(--seal); }
a:hover { color: #0b4a36; }
```

- **ブラウザ既定の青・訪問済みの紫を残さない。**
- 色だけに頼らない：本文中・フッターのリンクは下線（`text-underline-offset: 3px`）。
- ナビ・パンくず・記事一覧など「そこが全部リンクだと分かる場所」は下線省略可。
- 現在地は色ではなく罫線で示す（`aria-current="page"` に `inset 0 -2px 0 var(--seal)`）。

---

## Component Color Integrity

**汎用リンクの色指定が、部品固有の色を上書きしてはいけません。**

2026年9月に、記事内の主CTAが「緑地に緑文字」（コントラスト 1.00:1）で
表示されていた事故がありました。原因は詳細度です。

```
.article-body a   → 詳細度 (0,1,1)   ← 勝つ
.btn--primary     → 詳細度 (0,1,0)   ← 負けて白文字が消える
```

記事のCTAは `<article class="card article-body">` の内側にあるため、
本文リンクの色指定がボタンの文字色を上書きしていました。
同じことは `a:visited`（0,1,1）でも起こります。

### 守るルール

1. **汎用リンクセレクタは、クラスを持たない素のリンクに限定する。**
   ```css
   a:not([class]), a:not([class]):visited { color: var(--seal); }
   ```
   `a { }` や `a:visited { }` を裸で書かない。
2. **部品は自分の色を、通常 / visited / hover / focus-visible すべてで明示する。**
   `:visited` を書き忘れると、訪問済みで色が変わる部品ができる。
3. **`.article-body a` のような広いセレクタで、`.btn` や `.fit-cta` を巻き込まない。**
   巻き込みが避けられない場合は、同じファイルの後方で、より高い詳細度で打ち消す。
   現在の打ち消しは次の3つ（`articles.css` 末尾）。
   ```css
   .article-body .btn--primary, .article-body .btn--primary:visited { color: #fff; }
   .article-body .btn--ghost,   .article-body .btn--ghost:visited   { color: var(--ink); }
   .article-body .fit-cta,      .article-body .fit-cta:visited      { color: var(--seal); }
   ```
4. **新しいCTA・リンク部品を足したら、`article-body` の中でも実ブラウザで色を確認する。**
   CSSを読んで判断しない。**visited 状態も必ず確認する**（全リンクを踏んでから測る）。
5. **主要CTAは WCAG AA（通常4.5:1／大きい文字3:1）を維持する。**
6. `tools/audit-site.py` が、上の打ち消し3つと `a:not([class]):visited` の存在を検査する。
   消すと監査が落ちる。

## Buttons

| 種類 | 指定 | 使う場所 |
|---|---|---|
| `.btn--primary` | 地 `--seal` / 文字 #fff / 高さ約48px | 1画面に1つ。診断・カルテ作成 |
| `.btn--ghost` | 透明 / 1px `--ink-2` / 文字 `--ink` | 副次の遷移・PDF |
| `.btn--text` | 下線つきテキスト | リセットなど |
| `.field__guess` | 1px `--rule-strong` / min-height 32px | 補助操作 |
| `.share__btn` | 1px `--rule-strong` / min-height 44px | 共有 |

- 主要操作のタップ領域は **44px**、補助操作でも **32px** を下回らない。
- **広告のCTAに `.btn--primary` を使わない**（`.ad-card__link a` は緑の枠線ボタン）。

---

## Forms

- 全項目**任意**。空欄は0として扱い、その旨を明示する。必須マークを作らない。
- **ラベルと入力は常に縦に積む**（`.field { flex-direction: column }`）。
  横並びにすると2カラム時にラベルが1文字ずつ折れるため。
- `.field__label { min-width: 8em }` を外さない。
- 入力欄内で `outline: none` を書かない。フォーカスは `:focus-visible` の
  `outline: 2px solid var(--seal); outline-offset: 2px`。
- エラーを赤で出さない。0件送信は「まず1つだけ、金額を入れてみてください」と
  黒字＋罫線（`.form-note`）で伝える。
- **「わからない」（`.field__guess`）**：目安を入れたら
  「目安を入れました。実際の金額が分かれば変更できます」（`.field__hint`）を出し続ける。
  本人が値を変えたら解除。状態は `data-guessed` と `fixedCostChecker.guessed.v1` に持つ。

---

## Tables

```css
border-collapse: collapse;
th, td { border: 1px solid var(--rule); padding: 8px 10px; vertical-align: top; }
th { background: var(--seal-tint); font-weight: 500; }
.table-scroll { overflow-x: auto; }
.table-scroll > table { min-width: 30em; }
```

- **`min-width` を必ず与える。** 与えないと狭い画面で列が潰れ、スクロールもされない。
- 数値列は右寄せ＋`tabular-nums`。
- 出典と確認日を表の直下に置く。
- **順位列・★評価・「No.1」バッジを作らない。**

---

## Cards

`.card` は「1枚の紙」。背景 `--surface` / 1px `--rule` / radius 2px / 影なし。

- 1画面にカードを並べすぎない。**グルーピングはまず罫線で試す。**
- カードとアイコンで画面を埋めない（`.promise` は罫線だけで組んでいる）。

---

## Navigation & Reading Aids

到達しやすさと読みやすさのための部品。**すべて罫線・番号・余白だけで作り、画像は使いません。**
カードを増やさないという原則は維持します。

### `.themes` — 気になるテーマから見る（トップ）

トップのヒーロー直下。主要テーマへ**1クリック**で入る入口。
`地色 --surface / 下罫のみ`。行は `テーマ名（5.5em）＋ 説明 ＋ 件数バッジ`。
行全体がリンクで、最小高 44px。カードにしない。
テーマは6つまで。増えたらカテゴリへ寄せる。

### `.flow` — このサイトの使い方（トップ）

`家計チェック → 家計カルテ → 記事で確認 → 必要なら相談` の4段。
番号は `--seal` の円（23px）、段の間は 1px の線でつなぐ。
600px 未満は縦並び、600px 以上は横並び（`grid-template-columns: repeat(4, 1fr)`）。
末尾に「3までで終わることも、よくあります」を必ず添える。
**相談を最終ゴールに見せない**ための一文で、外さない。

### `.cluster` — 記事クラスターの順番ナビ（記事冒頭）

複数記事で1つの判断を支えるとき、記事の冒頭に置く。
番号つきの縦並びで、いま読んでいる回は `--seal` の塗り＋「いまここ」の印。
他の回へは1クリック。**「確認する順番」の図を兼ねる**ので、別に図を作らない。
リンクは最小高 40px。現在は保険5記事で使用（保険料見直し → 公的保障 → 保険証券 → 無料相談の仕組み → 3社比較）。

### `.takeaway` — この記事で分かること（記事冒頭）

`--seal-tint` の面＋緑の左罫。**箇条書き3点前後＋結論2〜4行**。
統計記事は先に結論、手順記事は何から始めるかを書く。
全記事に置く。本文を削る代わりではなく、読むかどうかを先に判断させるためのもの。

### `.next-read` — 次に読む（記事末尾）

末尾の関連記事。`2px --rule-dark` の上罫で区切り、見出しは「次に読む」。
一行の案内を添え、editorial list（`.article-list`）で並べる。カードにしない。

### `.cat-index` — カテゴリから探す（記事一覧の先頭）

カテゴリのアンカーリンクを横並びで。件数を添える。
`1px --rule-strong` の枠、最小高 44px。上下を罫線で挟む。

## Article

- 幅 680px、本文 16px、行間 1.95、見出しは明朝。
- **必須**：`.crumbs`／公開日・更新日／署名（家計の保健室 編集部）／`.sources`／
  診断CTA（`data-track="to-diagnosis"`）。`tools/audit-site.py` が存在を検査する。
- **出典・診断CTAの見出しは `<h2>` にしない。** `.section-label` を使う。
  本文のアウトラインは記事の内容だけで構成する。
  （クラス名 `.sources` `.to-diagnosis` `.crumbs` は監査依存のため**変更しない**）
- 出典・署名は 0.86〜0.88rem、`--ink-2` 以上。小さくしない。
- 広告は**1記事1枠**（監査で強制）。位置は診断CTAの後。

### 記事タイプ（`<body data-article-type>`）

新しい部品を作らず、既存の罫線・見出し・note-box の重みだけを変える。

| 型 | 値 | 見え方 |
|---|---|---|
| 統計記事 | `stats` | 表の上辺を太罫に、th を `--seal-tint`＋太字、`.note-box` に緑の左罫。<br>「重要な数値 → 平均は適正額ではない → 本文 → 出典」の順が一目で追える |
| 手順記事 | `steps` | h2 の左に短い緑の罫（診断UIの節番号と同じ調子）。<br>**手順番号はCSSで振らない**（見出しの文言がすでに持っているため） |
| 考え方記事 | `essay` | 部品を足さない。段落間を少し開けるだけ |

---

## Diagnosis（家計チェック）

- 所要と項目数を先に示す。全項目任意。
- 0項目のまま送信されたら**結果を出さない**（広告も0件）。
- 推測値（`guessed`）は必ず「サイトが入れた目安」と分かる状態を保つ。
- **診断ロジック（`CATEGORIES` / `benchmark` / `saving` / `diagnose`）を
  デザイン都合で変更しない。**

---

## Household Chart（家計カルテ）

```
02 家計カルテ    表題・作成日・世帯の前提・受付印／見直し余地（月・年）／
                 支出合計と固定費・変動費の内訳／推定である旨の注記
03 費目ごとの状態
04 何が起きているか（優先3件のみ展開・残りは details）
05 今日やること 3つ
06 もう少し詳しく知る（記事）
07 相談・比較（広告はここだけ）
08 持ち帰る（PDF・共有・印刷）
```

### 状態は4段階だけ

| ラベル | 条件 |
|---|---|
| 情報不足 | 未入力、または「わからない」で入れた目安のまま |
| 今は見直さなくてよい | 目安の 1.12 倍以内、または見直し余地なし |
| 確認する価値あり | 目安の 1.12 倍超、または目安が無く見直し余地あり |
| 優先して確認 | 見直し余地が大きい上位3件（`guessed` は選ばない） |

- **禁止語**：危険・異常・要治療・重症・診断名。比喩でも使わない。
- 状態は**色ではなく言葉**で示す。金額に色を付けない。
- 「優先して確認」「確認する価値あり」はゲージ＋目安との差＋記事リンクを出す。
  「今は見直さなくてよい」「情報不足」は**1〜2行に畳む**（ゲージを出さない）。
- 「確認する価値あり」の存在自体は、畳まずに費目一覧で見える状態を保つ。
- 読み上げは `#chart-status` の一行要約に限定する（結果DOM全体を読ませない）。

---

## Advertising

1. **広告は1箇所に集約する。** カルテは最大3件、記事は1枠（監査で強制）。
   例外は**比較記事**だけで、比較対象を横並びにする場合に限り3件まで置ける
   （下の Comparison を参照）。
2. **0件でよい。** 該当が無ければセクションごと出さない。必ず3件出すことはしない。
3. 選定に使うのは **診断結果との関連性・見直し余地・現在の契約状況**
   （`shouldShowAd()` の条件）だけ。**報酬額・確定率は使わない。**
4. `guessed`（目安値）だけを根拠にした広告は出さない。
5. セクション冒頭に、本文と同じ大きさ（0.94rem）で
   「ここから先は広告です。報酬の有無で順位や評価を変えることはしていません」。
6. 「広告」ラベルは 0.82rem 以上・枠線つき・`--ink-2` 以上。**警告色は使わない。**
7. 広告は `--seal-tint` の帯（`.ads`）に入れ、編集と面で分ける。
8. **「使わない選択肢」を必ず併記する**（自分で進める手順へのリンク）。
9. **ASP発行コードは1文字も改変しない。** URL・アンカーテキスト・img・
   `referrerpolicy`・`rel` を含め原文維持。装飾は外側のコンテナだけで行う。
10. `.ad-card` / `.ad-tag` / `data-ad-slot` は監査依存のため**改名しない**。

---

## Comparison（比較＝適合表）

比較は「どれが優れているか」ではなく「**どれが自分の条件に合うか**」を示す。

### 原則

- 1位・2位・3位を作らない／★評価を作らない／「おすすめNo.1」を作らない。
- **掲載順で優劣を示さない。** 並び順の根拠（五十音順など）を表の前に明記する。
- 報酬額・確定率を編集上の順序・評価に使わない。
- 各社のCTAは**色・サイズ・文言をすべて同じ**にする（`.fit-cta`）。
- **「どれも使わない／自分で進める」**（`.fit-optout`）を必ず置く。
- 無料の仕組み（誰が費用を負担しているか）を表より前に書く。

### 比較軸（行）

相談方法／オンライン可否／対応エリア／相談対象／加入義務／特徴／注意点／こういう人に向く

### マークアップ

```html
<p class="fit-table__order">掲載順は五十音順です。報酬額・確定率は順序や評価に使っていません。</p>
<div class="table-scroll fit-table">
  <table>
    <thead>
      <tr>
        <td></td>
        <th scope="col">サービスA<span class="fit-table__checked">確認日 2026-09-01</span></th>
        <th scope="col">サービスB<span class="fit-table__checked">確認日 2026-09-01</span></th>
      </tr>
    </thead>
    <tbody>
      <tr><th scope="row">相談方法</th><td>…</td><td>…</td></tr>
      <tr><th scope="row">注意点</th><td>…</td><td>…</td></tr>
      <tr><th scope="row">こういう人に向く</th><td>…</td><td>…</td></tr>
    </tbody>
  </table>
</div>
<p class="fit-table__note">各社の公表情報にもとづきます。条件は変更される場合があるため、必ず公式でご確認ください。</p>
<p class="fit-optout"><a href="…">相談やサービスを使わず、自分で進める手順を読む ›</a></p>
```

- 比較記事は「同一箇所に複数リンクを並べてよい唯一の場所」（`AFFILIATE_BACKLOG.md`）。
- **実案件のデータは、一次情報を確認してから入れる。** 推測で埋めない。

### CTA行（`.fit-row-cta` / `.fit-ad`）

比較対象の申し込みリンクは、表の**最終行の1つ手前**にまとめて置く。
CTAより先に比較情報を読み終える並びを崩さない。

- 器は `.ad-card` をそのまま使い（`.fit-ad` で余白だけ詰める）、
  各セルに `data-ad-slot` と「広告」表記を必ず持たせる。
- **3社のボタンは大きさ・色・余白・強調度をすべて同一にする。**
  1社だけ `.btn--primary` にしない。上に置かない。囲みを変えない。
- アンカーテキストは**ASP発行コードの原文**で、社名の長さが違う。
  放置すると行数の差でボタンの高さが変わり、それ自体が強弱になる。
  `min-height: 5.8em` で高さを固定してそろえる（幅は `table-layout: fixed` で均等）。
- ASP発行コードに `class` などの属性を足さない。装飾は外側の器だけで行う。
- コードは `config.js`（`insuranceComparison`）に**原文のまま**置き、記事側は差し込むだけ。
- 直後に「どれも使わず、自分で進める」（`.fit-optout`）を必ず置く。
- `tools/audit-site.py` が、3社ぶんのコードの原文一致・CTA枠が3つあること・
  表の中で1社だけ主ボタンになっていないことを検査する。

---

## Illustration

原則、絵は使わない。使う場合の唯一の用途は「手順の図解」。

- **禁止**：札束・コイン・貯金箱・右肩上がりの矢印・人物のストック写真。
- **許容**：単線・1色（`--ink` または `--seal`）の図解、表と罫線でできた図。
- 生活者向けの親しみは、イラストではなく**余白と文体**で出す。

---

## Icons

アイコンは原則使わない。「何でもカード＋アイコン」を避ける。
例外は3つだけ：**印章（保）／リンクの `›`／details の `＋−`**。絵文字は使わない。

---

## Mobile

- 基準 390px。**360px でも崩れないこと。**
- タップ領域 44px（補助操作は最低32px）、間隔8px以上。
- ヘッダーは sticky にしない。ハンバーガーを作らない。
  モバイルで1画面を大きく占有しないこと。
- 横スクロールを起こさない。広い内容（表）は自身の `overflow-x` の中でスクロールさせる。

---

## Accessibility

- 文字コントラスト **4.5:1 以上**。メタ・注記・免責・出典・広告ラベルも例外にしない。
- 操作部品の境界 **3:1 以上**（`--rule-strong`）。
- `:focus-visible` の outline を消さない。
- 全ページ先頭に**スキップリンク**（`.skip-link`、フォーカス時のみ表示）。
- `<main id="main">` を全ページに置く。
- フォームは `<label>` と入力を関連付ける。
- 結果は一行要約の live region に限定する。
- `@media (prefers-reduced-motion: reduce)` で scroll-behavior と transition を無効化。
- **ダークモードは提供しない。** 紙の帳票という比喩と両立せず、warm paper の価値を失う。
  必要になった時点で再検討する。

---

## Motion

- transition は**色と境界のみ**、120〜150ms、ease。
- 移動・拡大・フェードインを伴うアニメーションを作らない。
- `scroll-behavior: smooth` は reduced-motion で必ず解除する。

---

## Do

- 罫線と余白で階層をつくる
- 金額の前に「なぜ」を置く
- 不利な情報を先に書く
- 推定値は丸め、「目安」と併記する
- 広告は1箇所にまとめ、明示し、使わない道を併置する
- 該当しない人には出さない
- 迷ったら要素を減らす

## Don't

- 影・グラデーション・ピル形状・絵文字
- 金額を赤で見せる／朱を印章以外に使う
- 順位・★評価・No.1バッジ
- 免責・出典・広告表記を最小フォント／低コントラストにする
- 診断結果の直後に広告を置く
- カードとアイコンで画面を埋める
- sticky ヘッダー・ハンバーガー・SaaS風グローバルナビ
- 銀行のような冷たい配色／保険会社の青一色／AI風の紫グラデーション
- 投資家向けブラックUI／高級資産運用サービス風
- デザイン都合で ASPコード・診断ロジック・URL・canonical・
  sitemap・構造化データ・計測イベント名を触る
- デザイン改善のためだけにフレームワークやビルドを導入する

---

## この文書とコードの関係

| 対象 | ファイル |
|---|---|
| トークン・共通UI・診断・カルテ | `styles.css` |
| 記事・比較表・節ラベル | `articles.css` |
| カルテの生成・状態判定・広告選定 | `script.js` |
| 広告と計測の設定（ASP発行コードは原文） | `config.js` |
| 公開前チェック（CIで自動実行） | `tools/audit-site.py` |

正式ドメインは **https://kakei-hokenshitsu.com/**（`CNAME`・canonical・sitemap・
構造化データすべて統一済み）。URLは変更しない。
